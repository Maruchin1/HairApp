package com.example.navigation

enum class DestinationType {
    HOME,
    CARE_DETAILS,
    PEH_BALANCE,
    CARE_PHOTOS_GALLERY,
    SELECT_PRODUCT,
    PRODUCT_DETAILS,
    CARE_SCHEMA_DETAILS,
    CAPTURE_CARE_PHOTO
}